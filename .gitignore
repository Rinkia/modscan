# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / secrets
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Output
modding-docs/

# Showcase LLM response cache (the showcase OUTPUT is meant to be committed)
.showcase-cache/

# Local planning docs (kept out of the public repo)
.claude/

# Local scratchpad for throwaway spikes and analysis (never committed)
scratchpad/

# Installed JS toolchain + scan targets for the TS cross-check (npm install)
node_modules/

# Java benchmark sources unpacked from Maven Central (benchmarks/java/fetch.py)
benchmarks/java/*-*/

# Downstream plugin packages for the usage cross-check. Pinned in
# usage-crosscheck/downstream.json, installed with --target, never committed.
usage-crosscheck/downstream/
