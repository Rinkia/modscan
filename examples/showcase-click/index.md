# Modding Guide

> Generated by MODScan. Facts from static analysis; prose from an LLM; examples validated by loading them.

## Architecture Overview

# Architecture Overview for Modders

This codebase consists of 17 internal modules connected by 48 dependency edges. It exposes two kinds of extension points: **hooks** (functions you call to attach behavior) and **subclasses** (abstract classes you extend). Both categories are re-exported from the package's public entry point, so you can access them directly without reaching into submodules.

## Hooks

Hooks are public functions in `click.decorators` that follow a registration pattern. They are validated as importable callables.

- **`confirmation_option()`** — `click.decorators:380`. Takes no parameters.
- **`version_option(version)`** — `click.decorators:421`. Takes a `version` argument.

Both are re-exported from the package's public entry point.

## Subclasses

For deeper extension, the codebase provides an abstract base class meant to be subclassed:

- **`ParamType`** — `click.types:42`. An abstract class (`ABC`) with base `abc.ABC`. Extend this to define your own type. It is validated via subclass instantiation and is re-exported from the package's public entry point.

## Summary

| Extension point | Kind | How to extend |
|---|---|---|
| `confirmation_option` | hook | Call the function |
| `version_option` | hook | Call the function with `version` |
| `ParamType` | subclass | Subclass the ABC |

All three are available from the package's public entry point, making them the intended surfaces for building mods against this codebase.

## Extension Points

| Point | Category | Location | Example |
| --- | --- | --- | --- |
| `click.decorators:confirmation_option` | hook | click.decorators:380 | executed |
| `click.decorators:version_option` | hook | click.decorators:421 | executed |
| `click.types:ParamType` | subclass | click.types:42 | verified |

See [plugin-guide.md](plugin-guide.md) for how to build each one.
